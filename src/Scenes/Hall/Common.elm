module Scenes.Hall.Common exposing (Model, nullModel, initModel)

{-| Common module

This module is generated by Messenger, don't modify this.

@docs Model, nullModel, initModel

-}

import Lib.Env.Env exposing (Env, addCommonData)
import Lib.Scene.Base exposing (LayerPacker, SceneInitData(..))
import Scenes.Hall.LayerBase exposing (CommonData, nullCommonData)
import Scenes.Hall.LayerSettings exposing (LayerT)
import Scenes.Hall.MainLayer.Export as MainLayer
import Scenes.Hall.MainLayer.Global as MainLayerG
import Scenes.Hall.SceneInit exposing (initCommonData, nullHallInit)


{-| Model
-}
type alias Model =
    LayerPacker CommonData LayerT


{-| nullModel
-}
nullModel : Model
nullModel =
    { commonData = nullCommonData
    , layers = []
    }


{-| Initialize the model
-}
initModel : Env -> SceneInitData -> Model
initModel env init =
    let
        layerInitData =
            case init of
                HallInitData x ->
                    x

                _ ->
                    nullHallInit
    in
    { commonData = initCommonData env layerInitData
    , layers =
        [ MainLayerG.getLayerT <| MainLayer.initLayer (addCommonData nullCommonData env) layerInitData
        ]
    }
