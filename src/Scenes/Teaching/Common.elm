module Scenes.Teaching.Common exposing (Model, nullModel, initModel)

{-| Common module

This module is generated by Messenger, don't modify this.

@docs Model, nullModel, initModel

-}

import Lib.Env.Env exposing (Env, addCommonData)
import Lib.Scene.Base exposing (LayerPacker, SceneInitData(..))
import Scenes.Teaching.LayerBase exposing (CommonData, nullCommonData)
import Scenes.Teaching.LayerSettings exposing (LayerT)
import Scenes.Teaching.MainLayer.Export as MainLayer
import Scenes.Teaching.MainLayer.Global as MainLayerG
import Scenes.Teaching.SceneInit exposing (initCommonData, nullTeachingInit)


{-| Model
-}
type alias Model =
    LayerPacker CommonData LayerT


{-| nullModel
-}
nullModel : Model
nullModel =
    { commonData = nullCommonData
    , layers = []
    }


{-| Initialize the model
-}
initModel : Env -> SceneInitData -> Model
initModel env init =
    let
        layerInitData =
            case init of
                TeachingInitData x ->
                    x

                _ ->
                    nullTeachingInit
    in
    { commonData = initCommonData env layerInitData
    , layers =
        [ MainLayerG.getLayerT <| MainLayer.initLayer (addCommonData nullCommonData env) layerInitData
        ]
    }
